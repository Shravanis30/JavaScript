const score = 100;
console.log(score);

const balance = new Number(288000000);      // it makes the specific with description
console.log(balance);

const newnum = console.log(balance.toString());      //converts into string
console.log(balance.toPrecision(2));                 // use it carefully it is usually used to reduced vast decimal numbers
console.log(balance.toFixed());   
console.log(balance.toExponential());  
console.log(balance.toPrecision(2));   
console.log(balance.toLocaleString());   
console.log(balance.toLocaleString('en-IN'));    // as per indians
console.log(Number.MAX_VALUE);   
console.log(Number.MIN_VALUE);   
console.log(Number.MAX_VALUE);
console.log(Number.MAX_SAFE_INTEGER);   
console.log(Number.MIN_SAFE_INTEGER);      
console.log(Number.__proto__);   